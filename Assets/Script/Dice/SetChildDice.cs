using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SetChildDice : MonoBehaviour
{
    private Dice Dice;
    // Start is called before the first frame update
    private void Start()
    {
        Dice = GetComponent<Dice>();
    }

    public void SetContainerChild()
    {
        
    }
    
}
